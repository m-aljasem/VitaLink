import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.aljasem.vitalink',
  appName: 'VitaLink',
  webDir: 'www',
  // Custom icons are replaced during build process in codemagic.yaml
};

export default config;
